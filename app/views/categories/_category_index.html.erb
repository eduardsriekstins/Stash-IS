<div id="<%= dom_id category %>">

  <a href="<%= category_url(category) %>" class="text-decoration-none link link-dark"><%= image_tag(category.image_resized2) %>

  <h3 class="d-flex justify-content-center"> <%= category.name %> </h3>
  </a>
</div>